# Legacy compatibility wrapper.
#
# Prefer:
# - requirements-core.txt  (minimal)
# - requirements-full.txt  (SSE/HTTP + extras)
#
# Keeping this file for older docs/automation:
#   pip install -r requirements-mcp.txt

-r requirements-full.txt

